<mvux:FeedView x:Name="GriddedFeedView"
    Grid.Row="1"
    Source="{Binding GalleryImages}"
    Margin="3"
    CanDrag="False"
    AllowDrop="False">
  <DataTemplate>
    <GridView x:Name="ContentGridView"
              ItemsSource="{Binding Data}"
              ItemTemplate="{StaticResource StyledCardTemplate}"
              CanReorderItems="True"
              CanDragItems="True"
              IsItemClickEnabled="True"
              AllowDrop="True"
              SelectionMode="None"
              HorizontalAlignment="Stretch"
              HorizontalContentAlignment="Stretch"
              VerticalContentAlignment="Center">
      <GridView.Header>                  
        <Grid ManipulationMode="None" Background="{ThemeResource SurfaceInverseBrush}"
                CornerRadius="10,10,0,0"
                Padding="10">
          <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="100"/>
                <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <PersonPicture ProfilePicture="Assets/Images/logo.png"
                         Width="60"
                         Style="{StaticResource DefaultPersonPictureStyle}"
                         Grid.Column="0"/>
          <TextBlock Text="{Binding Path=Parent.DashboardDescription}"
                     Style="{ThemeResource TitleMedium}"
                     Margin="16,14,16,0"
                     Foreground="{ThemeResource OnSurfaceInverseBrush}"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Center"
                     TextAlignment="Center"
                     Grid.Column="1"
                     TextWrapping="WrapWholeWords"/>
          <Button Command="{utu:AncestorBinding AncestorType=mvux:FeedView,Path=Refresh}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Stretch"
                  Grid.Column="2">
          <SymbolIcon Symbol="Refresh"
                      HorizontalAlignment="Right"/>
          </Button>
        </Grid>                     
      </GridView.Header>
    </GridView>
  </DataTemplate>
</mvux:FeedView>
</Grid>
